import { CaseListView } from '@/modules/cases/components/CaseListView';

export const metadata = { title: 'Dashboard — Oaklet Provider' };

export default function DashboardPage() {
  return <CaseListView />;
}
